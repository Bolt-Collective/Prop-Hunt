@using Sandbox;
@using Sandbox.Network
@using Sandbox.UI;
@inherits NavHostPanel
@attribute [StyleSheet]

<root>
	<div class="navigator-canvas" slot="navigator-canvas">
		
	</div>
</root>

@code
{
	public MenuNavigationPanel()
	{
		DefaultUrl = "/";
		AddDestination("/", typeof(FrontPage));
		AddDestination("/lobby", typeof(LobbyPage));
		AddDestination( "/servers", typeof(ServerListPage) );
		AddDestination( "/servers/create", typeof(CreateServerPage) );
	}
}
